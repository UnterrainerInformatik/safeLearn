# Initial Repairs
Im architecture.md in der section Observations has Du folgende Punkte:

- ~~Reveal.js läuft in zwei Welten: package.json deklariert ^5.0.5 (gelockt auf 5.2.1), aber wrapInReveal lädt Reveal 3.7.0 von jsDelivr — CSS, Theme, JS, Notes-Plugin. Das npm-Paket wird nie referenziert. Die Präsentationsansicht hängt damit zur Laufzeit an einem externen CDN und an einer acht Jahre älteren Major-Version.~~ **Erledigt** durch den Change `bundle-reveal-locally`: `wrapInReveal` lädt das installierte 5.2.1-Paket über zwei neue Mounts (`/node_modules/reveal.js/dist`, `/node_modules/reveal.js/plugin/notes`), die Konfiguration ist auf die 5.x-API migriert (`plugins` statt `dependencies`, `headjs` und der Print-Stylesheet-Injector entfallen), und das `moon`-Theme liegt als Derivation `css/reveal-theme-moon.css` vor, deren Google-Fonts-`@import` durch lokales Lato (300/400/700) ersetzt ist. Damit ist auch der Google-Fonts-Punkt aus den Observations erledigt: eine Präsentation kontaktiert keinen fremden Host mehr, und `test/checks/presentation.js` prüft das an den tatsächlichen Requests des Browsers.
- ~~express.static liegt auf dem Applikationsverzeichnis (app.js:332). Jeder authentifizierte User kann /app.js, /utils.js, /obsidian.js und — sofern die Datei neben app.js liegt — /keycloak.json samt Client-Secret abrufen. Das Repo selbst ist sauber: Root-keycloak.json ist gitignored, die getrackten wysiwyg-container-*/keycloak.json enthalten nur Platzhalter.~~ **Erledigt** durch den Change `harden-deployment-surface`: statt des Applikationsverzeichnisses serviert `app.js` jetzt eine Allowlist einzelner Mounts (`/assets`, `/css`, `/md`, `/obsidian-page.js`, die vier `logo-*.png`, `/index.html` und die beiden mermaid-`dist`-Verzeichnisse). `test/checks/deployment-surface.js` prüft beide Richtungen.
- ~~Session-Secret ist hartcodiert in keycloak-middleware.js:53, identisch in jedem Deployment aus dieser Quelle. Dazu MemoryStore als Session-Store.~~ **Erledigt** durch den Change `harden-deployment-surface`: das Secret kommt aus `SESSION_SECRET`, ohne Fallback — ein Deployment ohne die Variable startet nicht. Der `MemoryStore` bleibt bewusst, mit seinem Preis in `docs-building.md` festgehalten: ein Container, jeder Restart loggt alle aus, eine zweite Instanz erfordert vorher einen anderen Store.
- ~~/hot-reload ist vor checkAuthenticated registriert und damit unauthentifiziert erreichbar (liefert nur Pfade und Reload-Typ, keinen Inhalt).~~ **Vom Autor gestrichen** (siehe unten): das ist Absicht und bleibt so. Der Change `close-initial-repairs` hat an der Position des Endpunkts nichts geändert, sondern nur den ungeschützten `JSON.parse` von `req.query.context` abgesichert — ein Kontext, der sich nicht lesen lässt, gilt jetzt als keiner.
- ~~docs-permissions.md sagt „There is no role student" — getLdapGroups mappt OU Students aber explizit auf student.~~ **Erledigt** durch den Change `harden-role-set-assembly`: docs-permissions.md nennt `student` jetzt als Rolle aus der LDAP-Unit, korrigiert das Mapping `Students` → `student` und dokumentiert `students` als Alias.
- ~~wrapInReveal verlinkt /obsidian-page.css, das es nicht gibt; jede Präsentation feuert einen Request ins Leere.~~ **Erledigt** durch den Change `bundle-reveal-locally`, der den Link beim Umschreiben von `wrapInReveal` entfernt hat; nachgetragen und in `AI/architecture.md` durchgestrichen durch den Change `close-initial-repairs`. Der Eintrag `/obsidian-page.css` in der Known-Dangling-Liste von `test/checks/presentation.js` bleibt: er ist ein Fixture des Selbsttests dieser Liste, keine Ausnahme für eine noch bestehende Referenz.
- ~~Dazu cors deklariert aber nie importiert, safe-learn als eigene Dependency, .nvmrc: 20 vs. Dockerfile: node:25.6.0, ein paar tote Exports und sechs weitere Doku/Code-Divergenzen.~~ **Erledigt** durch den Change `close-initial-repairs`: `cors` und der Selbstverweis sind aus `package.json` raus (vorher repository-weit gesucht, inklusive `wysiwyg-container-*/` und `http/`), `.nvmrc`, `Dockerfile` und das neue `engines`-Feld nennen einheitlich Node 24 als Linie, `uiConfig`, `hasAllRoles` und `mdFilesDirOnHdd` sind entfernt und `jwtDecode`, `issuerUrl` und `keycloakIssuer` haben ihr `export` verloren. Dazu kamen die Reparaturen, die beim Aufräumen dazugehörten: `#practice` ist jetzt die Negation der `#exam`-Regel, `getDirectoryListing` verträgt eine leere Dateiliste, `preReplaceObsidianFileLinks` schneidet den Pfad nicht mehr ab, und `CTRL+ALT+D` trifft auch das große `D`. Acht Doku-Divergenzen sind korrigiert, die ASCIIDoc-/GitHub-Pages-Ankündigung ist zurückgezogen.

Davon streiche bitte /hot-reload raus. Das ist Absicht und bleibt so.

Die anderen Dinge kannst du bitte als propose verpacken und reparieren.