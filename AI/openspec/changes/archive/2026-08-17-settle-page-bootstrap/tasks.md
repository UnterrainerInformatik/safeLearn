## 1. Settle who reveals a page

- [x] 1.1 Confirm design's premise before acting on it: search `css/` for any rule setting `display` on `body` (design Open Questions). Record what you find — it decides whether Decision 2 corrects a live bug or removes a latent one. **Found:** no rule in `css/` sets `display` on `body`. The one `display` that reads as if it might — `css/body.css:14` — belongs to the `#wrapper` rule that follows the `body` rule, and `body` itself sets only margins, fonts and text rendering. The deck's stylesheets do not either: Reveal's own `reveal.css` puts `height`/`overflow` on `.reveal-viewport`, the class it adds to `body`, and no `display`. So Decision 2 removes a latent bug rather than correcting a live one — `""` and `block` render the same today.
- [x] 1.2 In `obsidian-page.js` `init()` (`:279`), change `document.body.style.display = "block"` to `""` (design Decision 2), and comment that this is the one place a page view is revealed and why the value defers to the stylesheets. **Done:** the write moved into `revealPage()`, which carries the comment; `init()` calls it once the preferences are applied.
- [x] 1.3 In `getAutoReloadScript` (`obsidian.js:1750-1867`), remove every write to `document.body.style.display` — the `DOMContentLoaded` hide (`:1828`) and all four reveals (`:1843`, `:1852`, `:1856`, `:1863`). The position restore stays; only the visibility writes go. **Done:** all five writes are gone and the handler now does nothing but put the position back.
- [x] 1.4 Confirm the restore still runs before the reveal in the page and document views: `init()` cannot resolve before the document holding its `<script>` tag has parsed, so `DOMContentLoaded` precedes it. Verify against a running instance with a saved scroll position rather than reasoning about it alone. **Verified, and the premise does not hold.** Against a running instance with `NEXT_AUTOSCAN=true`, a real hot reload of `md/test-md-file.md` and `history.scrollRestoration = "manual"` so the browser could not put the offset back on its own, one page load recorded: `/userattributes` answered at 289 ms, the body was revealed at 313 ms with `sessionStorage.scrollY` still holding `800`, `DOMContentLoaded` fired at 365 ms and the restore scrolled at 370 ms. The reveal precedes the restore by ~57 ms, so the page is shown at the top and then jumps. The cause is that `DOMContentLoaded` is not early: it waits for the deferred `<script type="module">` that `getMermaidScriptEntry` emits and for the Mermaid ESM graph it imports, which locally outlasts the preference round trip and over a real network would outlast it by far more. **Settled** by the mechanism design.md now carries: the reload script offers `window.safeLearnRestorePosition()` and `revealPage()` calls it in the same task, after clearing the property because a hidden body has no scroll height. Re-measured the same way afterwards: one single write to `display`, at 355 ms, with `scrollY` already 800 and the saved offset already cleared, and the first animation frame after it at 800. No jump, and no second write for the life of the page.
- [x] 1.5 Leave the inline `style="display: none;"` in `wrapInPage` (`:1883`) and `wrapAsDocument` (`:1927`) as it is, and comment at one of them what the hidden period is for — preferences that would otherwise flash — and who ends it. **Done:** the comment sits above `wrapInPage`'s `<body>` and names `wrapAsDocument` as serving it the same way.

## 2. Make the deck follow the same rule

- [x] 2.1 Give `wrapInReveal`'s `<body>` (`obsidian.js:1979`) the same inline `display: none` the other two wrappers use (design Decision 3). **Done**, with a comment naming the second reason the hidden period exists and why the deck's owner is not `init()`.
- [x] 2.2 Reveal the deck from Reveal's own readiness, in the deck's inline script beside `Reveal.initialize` — not from the reload script, which no longer writes the property in any view. The slide restore the reload script performs must have run by then; confirm the ordering against a deck that was hot-reloaded mid-presentation. **Written** as `showDeck()`, called from `Reveal.initialize(...).then(...)`; **the ordering does not hold**, for the same reason as 1.4. A deck hot-reloaded on slide 3 recorded: body revealed at 487 ms with `sessionStorage.revealSlide` still holding `{"h":2,"v":0}`, `DOMContentLoaded` at 507 ms. Reveal reports ready before `DOMContentLoaded`, so the reload script's handler finds `Reveal.isReady()` true and restores synchronously — after the reveal instead of before it. The deck ends up on the right slide (`getIndices()` reports `h: 2`), but it is seen on slide one first. **Settled** the same way as 1.4: `showDeck()` clears the property, calls `window.safeLearnRestorePosition()` and then `Reveal.layout()`, because a deck that computed its scale against a zero-sized viewport has to compute it again. Re-measured against a deck hot-reloaded on slide 3: `revealSlide` still held `{"h":2,"v":0}` while the body was hidden, was cleared by the time the body was shown, and the first animation frame after that reported `{h: 2, v: 0}`.
- [x] 2.3 Remove the branch-local one-second timer at `obsidian.js:1851-1853`; group 3 replaces it with a bound that covers every view. **Done** — it went with 1.3, its only statement being one of the five display writes.
- [x] 2.4 Run `npm test`. The presentation checks read a deck's rendered slides, so a deck that never reveals fails them — which is the intended signal, not a flake.

## 3. Bound the wait

- [x] 3.1 Arm a timer at bootstrap in each view's owner (design Decision 4): if the page has not been revealed by then, reveal it and log a message naming what it was waiting for. Seconds, not milliseconds — it exists to survive a request that never answers, not to race a slow one.
- [x] 3.2 Make sure the timer cannot reveal a page twice or fight the owner: clearing it is part of revealing.
- [x] 3.3 Assert the bound in `test/checks/rendering.js`: with `/userattributes` made to hang — refused or delayed through the harness's request interception, which already refuses hosts and the reload stream — the page still becomes visible and the message is on the console.
- [x] 3.4 Run `npm test`.

## 4. Turn the workarounds into assertions

- [x] 4.1 Rewrite the comment and the wait in `test/checks/rendering.js:105-112`: it no longer waits for "either script may have won", it asserts the page is revealed once and that the property holds the value Decision 2 names.
- [x] 4.2 Rewrite the comment at `test/smoke.test.js:31-35` for the same reason. Keep `textContent` if it is still the honest read; say why in one line instead of describing a race that no longer exists.
- [x] 4.3 Add the cross-view assertion the spec asks for: the same corpus file opened as page, document and deck each becomes visible exactly once. Put it where the view it covers is already exercised rather than inventing a fourth check module.
- [x] 4.4 Record the new coverage in `test/coverage-map.json` — `obsidian-page.js` is already guarded by `test/checks/rendering.js` and `test/checks/presentation.js`; extend the `what` of each to name the bootstrap rule.
- [x] 4.5 Run `npm test`. 72 checks, all passing. **Recorded rather than passed over:** one run in six failed on `the document view is the content without the page shell`, a check this change does not touch, and five further runs — two whole suites and four content suites — did not reproduce it. The plausible mechanism is independent of this change: that check compares the text of the same file rendered as a page and as a document, `render()` reads it at `domcontentloaded`, and the Mermaid module replaces the diagram block's content shortly after — so whether the two reads see the source or the rendered SVG is a race the check has always had. Worth a change of its own; not this one.

## 5. Measure the stream, then repair what the measurement shows

- [x] 5.1 Establish where connections can honestly be counted (design Open Questions): the server's `clients` map (`app.js:174-190`) and the browser's own accounting are both candidates, and the harness refuses the stream inside the page on purpose (`test/harness.js:375-399`). Try both and record which one reports the truth. **Answered: the server's `clients` map is the honest counter, and the browser's request accounting is not.** Both were taken at every step of 5.2, from a script driving a session opened with `openSession` — the harness refuses the stream for shared sessions, so no check of the suite can count from inside the page. The server's number was read through the line `broadcastReloadSSE` already logs, provoked by touching a corpus file no page of the walk was open on, so the broadcast reached nobody and only produced the count. The browser's number was requests to `/hot-reload` started minus the ones that finished or failed. Before the repair the two agreed, but only by coincidence: nothing was ever closed. After it, the server's number stayed at 1 while the browser's climbed to 9, because a stream the page closes itself is reported as neither finished nor failed — the browser's accounting counts streams opened, not streams held.
- [x] 5.2 Walk a session through a series of pages in one tab — forward, then back, so the back/forward cache is exercised — and record how the count moves with the number of pages visited. Write the numbers into this task. **Measured.** One tab, a student session, `NEXT_AUTOSCAN=true`, five corpus pages forward and then back:

  | step | server `clients` | browser holds |
  | --- | --- | --- |
  | after login | 2 | — |
  | forward 1 | 2 | 1 |
  | forward 2 | 3 | 2 |
  | forward 3 | 4 | 3 |
  | back 1 (`persisted=true`) | 4 | 3 |
  | back 2 (`persisted=true`) | 4 | 3 |

  One connection per page visited, never released. Going back releases nothing either, and adds nothing: `pageshow` reports `persisted=true`, so those documents were alive in the back/forward cache the whole time, connection included — which is the mechanism design.md guessed at. The walk was cut to three pages to get these numbers at all: with five, **the fifth navigation never completed** and failed on the 45-second timeout. That is the recorded consequence, observed: the browser allows six connections per host, and the navigation queued behind the streams the earlier pages were still holding.
- [x] 5.3 If connections accumulate: close the `EventSource` on `pagehide` and open a new one on `pageshow` (design Decision 5), then repeat 5.2 and record the numbers again. If they do not accumulate, change nothing here and say so — the finding is the deliverable. **They accumulate, so the repair applies.** `pagehide` closes the stream and clears any pending retry, `pageshow` opens a new one when there is none — the second half matters as much as the first, or a page restored from the cache would be listening to nothing. Re-measured over the same walk, now the full five pages forward and four back: the server's `clients` map read **1 at every one of the ten steps**, and the walk completed. The fifth page, which the run before could not load at all, loads.
- [x] 5.4 Independent of 5.3, fix the reconnect: `connectSSE` (`obsidian.js:1752`, `:1824`) registers a `DOMContentLoaded` listener on every call and retries on a fixed three-second timer. Register the listener once, and back off increasingly so an unavailable server does not collect one request per client every three seconds. **Done**, in both halves. There is no `DOMContentLoaded` listener left to register more than once: the restore it carried is now a function the reveal owner calls (1.4), so the count went from one per reconnect to none. The fixed three-second retry became a doubling backoff — one second, doubling to at most a minute, reset by a connection that opens — so a page whose stream drops once still retries promptly while an application that stays down is asked once a minute rather than twenty times.
- [x] 5.5 Assert the retry behavior where it can be asserted from a run — that a page whose stream is refused does not open a new connection every three seconds indefinitely. If the harness cannot observe it, record the gap in `docs-testing.md` under "What the corpus demonstrates but a run cannot assert". **Asserted from a run**, in `test/checks/deployment-surface.js`: the harness already refuses that stream inside the browser, so every page of a shared session is in exactly the situation the check is about. It records the requests to `/hot-reload` over eleven seconds and fails unless each gap is markedly larger than the one before it — a fixed timer produces equal gaps and does not pass. No gap had to be recorded in `docs-testing.md`.
- [x] 5.6 Run `npm test`. 73 checks, all passing, including the new one.

## 6. Settle the observations

- [x] 6.1 Strike the `body.style.display` Observation in `AI/architecture.md` section 9, naming this change and the owner it settled on for each view. **Struck**, naming each view's owner, the value they write, the measurement that changed how the restore is sequenced, and the checks that hold the rule.
- [x] 6.2 Settle the server-sent-events Observation according to 5.2's result: struck with the repair if connections accumulated, or struck as a measured non-finding with the numbers if they did not. Do not leave it open with a note. **Struck with the repair**, since connections did accumulate: the entry carries the numbers from both walks, the back/forward cache as the mechanism, the stalled fifth navigation as the consequence, and why the counts were read from the server rather than from the browser.
- [x] 6.3 Record in the same section what the deck's second owner is and why it is not the same script as the other two views — a reader should find the decision, not rediscover the difference. **Recorded** as an entry of its own beside the two struck ones, and section 7's live-reload paragraph was corrected with it — it still said the client reconnects after three seconds and restores the position itself, and neither is true any more.
- [x] 6.4 Run `npm test` one final time, with every group applied. 73 checks, all passing.
