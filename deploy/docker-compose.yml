version: '3'
services:

  test:
    image: gufalcon/safe-learn:latest
    container_name: safe-learn
    restart: unless-stopped
    ports:
      - "1112:8080"

networks:
  default:
    external:
      name: proxy_default