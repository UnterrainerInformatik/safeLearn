{
  "description": "Which check guards which file. The single source for the coverage record: docs-testing.md points at this file instead of restating it, test/coverage.test.js keeps it honest, and AI/claude/hooks/name-covering-checks.mjs reads it to name the checks when a guarded file is edited. Paths are relative to the repository root.",
  "checks": {
    "test/smoke.test.js": {
      "what": "The harness itself: both roles reach authenticated content, two sessions stay independent in either order, and the two sessions really are different users.",
      "guards": [
        "test/harness.js",
        "app.js",
        "middlewares/keycloak-middleware.js",
        "keycloak.json"
      ],
      "corpus": [
        "md/test-perms.md",
        "md/test-md-file.md"
      ]
    },
    "test/content.test.js": {
      "what": "Wiring for the content suite: imports every check module so the whole suite runs in one process, and asserts that a run logs in once per role.",
      "guards": [
        "test/harness.js",
        "test/checks/permissions.js",
        "test/checks/views.js",
        "test/checks/rendering.js",
        "test/checks/legibility.js",
        "test/checks/navigation.js",
        "test/checks/presentation.js",
        "test/checks/directory-search.js",
        "test/checks/deployment-surface.js"
      ],
      "corpus": []
    },
    "test/checks/permissions.js": {
      "what": "Block-level and whole-file directives in both directions, including the admin directive and both plural aliases (teachers and students), their effect on the navigation tree, time-bounded windows, and the teacher-to-student downgrade. That the navigation tree and the page a link leads to agree for every file carrying a whole-file directive - the tree read from the rendered sidebar, the page from a direct request, compared against each other rather than against a derived expectation - and that the two refusals stay told apart by their cause. That the same rule reaches Markdown posted to /convert, which has no corpus file behind it. That a rendered page costs the identity provider at most one account lookup however many directives it evaluates, and that a request evaluating no directive costs none - counted on the session by fetchAccount, because those lookups happen between the application and Keycloak where the browser cannot see them.",
      "guards": [
        "utils.js",
        "obsidian.js",
        "app.js",
        "middlewares/keycloak-middleware.js",
        "permission-context.js",
        "test/harness.js"
      ],
      "corpus": [
        "md/test-perms.md",
        "md/test-perms-teacher-alias.md",
        "md/test-perms-student-alias.md",
        "md/test-fileperms-teachers.md",
        "md/test-fileperms-4bhif-5bhif-2ahif.md",
        "md/test-fileperms-window-closed.md",
        "md/test-md-file.md",
        "md/test-search-teacher-file.md",
        "md/test-search-staleness-restricted.md"
      ]
    },
    "test/checks/views.js": {
      "what": "The #exam, #practice and #answer pseudo-roles, driven through the ve and va preferences.",
      "guards": [
        "utils.js",
        "obsidian.js",
        "app.js"
      ],
      "corpus": [
        "md/test-exam-practice-question.md"
      ]
    },
    "test/checks/rendering.js": {
      "what": "Callouts and their collapsible forms, highlighted code, fragment indices, side-by-side columns, the image forms, and the emitted PlantUML and Mermaid markup. That a page view is shown by its one owner with the value that defers to the stylesheets, and that the wait for it is bounded: a preference request left unanswered still produces a visible page and a console message naming what it waited for, while a page that came up normally reports nothing.",
      "guards": [
        "obsidian.js",
        "app.js",
        "obsidian-page.js",
        "css/callouts.css",
        "css/main.css",
        "test/harness.js"
      ],
      "corpus": [
        "md/test-md-file.md",
        "md/assets/test-local-image.jpg",
        "md/assets/Pasted image 20240315152828.png"
      ]
    },
    "test/checks/legibility.js": {
      "what": "What a rendered page presents to the person reading it, read off the page rather than out of a stylesheet: that nothing casts a shadow behind the prose or overrides the platform's rasterisation, that the page and document views declare a viewport laying the page out at the device's width without pinning the scale while the presentation view keeps its pinning, and that a 390px viewport really gets the narrow layout - the navigation folded away, the burger shown, and nothing pushing the page sideways. That every font family css/ names resolves to one this deployment declares, a generic, or a named system face, with the shipped names read from the @font-face block the renderer emitted rather than from a list kept in step by hand, and that a chain reaching for a shipped face ends in a generic. That the bar above the page, the navigation column and the menu render in one font. That inline code and a code block share one face at one size, both in fixed proportion to the reader's chosen size and following it when it changes, and that neither breaks a long token mid-word. That both themes clear WCAG AAA for body text as a measured ratio, and that the dark one is neither pure white nor at the maximum its background could reach. That a link in the text looks the same before and after it is followed, in both themes, and is underlined - the visited state computed from the page's own CSSOM, because Chrome answers getComputedStyle for a visited link with its unvisited style, and that computation held against the browser on the state the browser will report. That bold and italic on a rendered page are cuts this deployment ships rather than the browser's smear and shear: every file in the two font directories is read out of its own OS/2 table and held against the cut its name claims - the weight and the italic bit, through the sfnt, woff and woff2 containers the scanner accepts - and every family the renderer declares has a 700 face and an italic face, except the typefaces obsidian.js records as having none upstream. That the two chains applyAttributes() writes onto the page as inline styles begin at the family getFontImports() declared and end in the generic that typeface belongs to - a serif behind a serif, a monospace behind a monospace - which no stylesheet can supply, because an inline style outranks them all. That the deployment nominates the two typefaces a reader who has chosen none reads in, by name rather than by position: that each nomination is one the picker it belongs to actually offers - a nomination nobody offers is fallen past in silence onto whichever font sorts first - and that an attribute block carrying no font choice, which is what a reset account hands the page, resolves through the page's own chosenTypeface to those two rather than to a numeric default or to the alphabet. And that a position stored as a string rather than a number, as 20 of the accounts carrying preferences in the production realm did, falls to the nomination instead of being guessed at, because the page cannot tell a stringified position from the name of a font no longer shipped.",
      "guards": [
        "css/body.css",
        "css/markdown.css",
        "css/movement.css",
        "css/fixes.css",
        "css/topbar.css",
        "css/media.css",
        "obsidian.js",
        "test/harness.js",
        "obsidian-page.js",
        "assets/main-fonts",
        "assets/nav-fonts",
        "css/reveal-theme-moon.css"
      ],
      "corpus": [
        "md/test-md-file.md"
      ]
    },
    "test/checks/navigation.js": {
      "what": "Wiki-link resolution including the duplicated basename — with a digit in the name and without one — and the name with spaces, the folder-name edge cases, the table of contents and the navigation tree.",
      "guards": [
        "obsidian.js",
        "app.js"
      ],
      "corpus": [
        "md/test-md-file.md",
        "md/test-presentation.md",
        "md/presentations/test-presentation.md",
        "md/test-chapter-10.md",
        "md/presentations/test-chapter-10.md",
        "md/presentations/test - name - with - spaces.md",
        "md/folder-tests/folder with spaces/Untitled.md",
        "md/folder-tests/special chars $'&`´!/Untitled.md",
        "md/folder-tests/a very long folder with spaces oh my god i cannot believe how long it is/Untitled.md"
      ]
    },
    "test/checks/presentation.js": {
      "what": "The deck built from the source's heading structure, fragments in a deck, the deck's own references including the known-dangling list, that it contacts no host but the application and the identity provider, that a host a stylesheet asks for is recorded even when refused, the document view, and the documented shortcut into it with the shift key held and without. That one corpus file opened in all three views becomes visible exactly once in each, by that view's own owner and with the value that defers to the stylesheets - read as the sequence of values the property takes, so a second script writing it fails the run.",
      "guards": [
        "obsidian.js",
        "app.js",
        "obsidian-page.js",
        "css/reveal.css",
        "css/reveal-theme-moon.css",
        "test/harness.js"
      ],
      "corpus": [
        "md/presentations/test-presentation.md",
        "md/test-md-file.md"
      ]
    },
    "test/checks/deployment-surface.js": {
      "what": "What a running instance publishes over HTTP: that keycloak.json, the application sources, the dependency manifests and what sits beside a mounted package directory are not served and a refusal looks like a path with no file behind it, that the assets a page addresses still are — including the ones only a stylesheet names — that every same-origin reference of a rendered page resolves in all three views, and that the one endpoint answering before authentication survives a parameter it cannot interpret. That a page which cannot reach the reload stream keeps trying with a growing delay rather than on a fixed timer. That a self-authenticating route — the directory search endpoint and the status endpoint beside it — returns no directory data to a request carrying neither a bearer token nor, distinctly, to one carrying only a valid browser session.",
      "guards": [
        "app.js",
        "obsidian.js",
        "middlewares/keycloak-middleware.js",
        "middlewares/directory-service.js",
        "keycloak.json",
        "assets/main-fonts/Lato.ttf",
        "assets/main-fonts/Lato-300.ttf",
        "assets/main-fonts/Lato-700.ttf"
      ],
      "corpus": [
        "md/test-md-file.md"
      ]
    },
    "test/checks/directory-search.js": {
      "what": "GET /api/admin/directory/search: a teacher token searching by name and by a role/group value that is not a class or teacher/admin, an admin token succeeding without holding teacher, a student token refused, a missing bearer refused, an invalidated (expired/revoked-shaped) token refused indistinguishably from a missing one, a match's response carrying only name and roles, and an empty or unmatched query answering with an empty list rather than an error. That a search the server cannot answer yet reports the running fetch (202) instead of being held open, and is answered once the caller repeats it. GET /api/admin/directory/status: a teacher token reading what the directory holds without transferring any of it, the payload carrying figures only and no record from the directory, and a student token refused indistinguishably from a request carrying no identity at all.",
      "guards": [
        "app.js",
        "middlewares/directory-service.js",
        "middlewares/keycloak-middleware.js",
        "test/harness.js"
      ],
      "corpus": []
    },
    "test/checks/search.js": {
      "what": "What a search may return and what it may never reveal: that a term occurring only in a file whose first line restricts it, only in a restricted block, only in a block addressed to another person by name, or only across the seam a removed block leaves behind, comes back as nothing - and as the same nothing a term that is in no file at all comes back as. That the joined document really does contain the seam term, so the seam check is about a seam. That the same session running the same query before and after the teacher/student toggle sees its results change exactly as the rendered page does. That a file restricted after the last scan is not returned and one relaxed after it is, driven by editing the corpus while the application runs. That an answer carries the results and nothing else - no total, no mark for what was left out - and that every occurrence it reports is an occurrence the session can count on the page it is served. That following a result lands on its heading, that a repeated heading lands on the occurrence the search found, and that a hand-written target naming a heading the session may not see leaves the page at its top. That a query below the minimum length is refused, that a request carrying no session is refused, that matching is substring and case-insensitive, that a name or heading match outranks a body match, and that a whole query costs the identity provider at most one account lookup. That neither switch a reader may set for themselves opens the exam variant to them: a student that turns the exam view on, and a teacher that turns it on while in the student view, are told nothing about the `@@@ #exam` block, and every session is offered exactly one of the exam and practice variants across both roles and both settings. That `#answer` is a view and not a restriction - `va` alone decides it, for a student exactly as for a teacher - stated here so it is not mistaken for one. And that a block whose window has closed is found by nobody, the teacher session included, since a closed window is the one directive `admin` does not short-circuit, in both the block form the segment filter decides and the whole-file form `resolveFileVisibility` decides.",
      "guards": [
        "obsidian.js",
        "app.js",
        "obsidian-page.js",
        "css/search.css",
        "utils.js",
        "permission-context.js",
        "test/harness.js"
      ],
      "corpus": [
        "md/test-search-blocks.md",
        "md/test-search-teacher-file.md",
        "md/test-search-jump.md",
        "md/test-search-staleness-open.md",
        "md/test-search-staleness-restricted.md",
        "md/test-search-views.md",
        "md/test-fileperms-window-closed.md"
      ]
    },
    "test/checks/search-keyboard.js": {
      "what": "Driving the search from the keyboard: that arrow-down enters the list at its first result and stops at its last node without wrapping, that exactly one node carries the mark at a time and that the field names the same node it stands on, that arrow-right opens a result and then steps into its headings while arrow-down walks them and steps out to the result below, that arrow-left closes a heading list onto the result it belongs to and hands the selection back from a result that is not open, that arrow-up at the first result hands it back too and that arrow-left afterwards moves the caret within the query again. That arrow-right at the end of the query resumes the node the selection was handed back from while arrow-down starts at the top whatever happened earlier, that a further character typed forgets that node, and that anywhere but the end of the query arrow-right stays the caret - a selected query collapses, a caret inside it moves one character, and a query nothing answers has nothing to step into. That enter leaves the reader exactly where a pointer following the same node leaves them, compared against the click rather than against a rebuilt expectation, for a result and for a heading. That a click into the field selects the query it holds, that arrow-right then leaves the caret at its end so the next character extends it, and that a pointerdown carrying pointerType touch before the focus selects nothing. That entering the list, walking it and leaving it make no request to /search and leave the query alone, that a walk moves neither the caret within the query nor the page behind the sidebar, and that the Ctrl+Alt print shortcut still works with the caret in the field. That expanded is one state whichever of the chevron and the arrow keys set it, in both directions, and that a new answer - narrower, or below the minimum length - gives the selection up. The two shapes the walk needs are asserted before they are relied on, so a corpus edit fails the fixture rather than weakening the checks.",
      "guards": [
        "obsidian-page.js",
        "obsidian.js",
        "css/search.css",
        "css/fixes.css",
        "test/harness.js"
      ],
      "corpus": [
        "md/test-search-jump.md",
        "md/test-md-file.md"
      ]
    },
    "test/directive-grammar.test.js": {
      "what": "What a permission directive means, entry by entry: a table of edge-case directive lines read by parseFirstLineForPermissions, covering the permissive cases of parseLocalDateTime that a hand-written mirror gets wrong. It is the oracle the companion plugin's own grammar is held against - the plugin's parseDirectiveEntries reads the same table and has to reach the same conclusion per entry, so a divergence fails here rather than misinforming a person writing a document. Needs no browser, no login and no server; it does need the plugin checkout the AI/plugin symlink points at.",
      "guards": [
        "obsidian.js",
        "utils.js"
      ],
      "corpus": []
    },
    "test/semester-table.test.js": {
      "what": "The dates a semester table is made of: the walk from the first lesson to the last, the weekdays it keeps, and the shape of the table written from them. Taken out of the plugin's own main.ts rather than reimplemented, so the check cannot drift away from the code it is about. Covers the cases that are invisible while writing it - both ends of the range, the daylight-saving switch a semester crosses twice, the turn of the month and of the year, and five timezones that have to produce one identical table because the corpus is shared. Also that the generated Markdown is a table to a GitHub-flavoured renderer, standalone and under a paragraph. Needs no browser, no login and no server; it does need the plugin checkout the AI/plugin symlink points at. What the command does to a document is in test/obsidian/plugin.test.js, which drives the real editor.",
      "guards": [
        "app.js"
      ],
      "corpus": []
    },
    "test/coverage.test.js": {
      "what": "The coverage record itself: every path it names exists, every test file appears in it, and no entry names a file that is gone.",
      "guards": [
        "test/coverage-map.json"
      ],
      "corpus": []
    },
    "test/directory-service.test.js": {
      "what": "A regression guard for the Keycloak admin-API pagination fix: fetchAllUserPages keeps asking for pages until one comes back short, rather than silently stopping at the first (a directory larger than one page going quietly missing past the 100th user). Stubs fetch; needs no live realm, no browser, no login.",
      "guards": [
        "middlewares/directory-service.js"
      ],
      "corpus": []
    },
    "test/permission-context.test.js": {
      "what": "That a request resolves its permission context once: many checks of one request cause one account lookup, concurrent checks started before the first resolves cause one, and a request evaluating no directive causes none. That the downgraded check and the check beside it do not affect each other in either order, that a role inherited from Object.prototype - constructor, __proto__ - is granted to nobody, that a failed lookup settles the whole request on the default preferences with its roles intact and is logged once, and that every directive of one request is decided against the same reference time even when a window closes mid-request. Stubs fetch; needs no live realm, no browser, no login.",
      "guards": [
        "permission-context.js",
        "utils.js",
        "middlewares/keycloak-middleware.js",
        "obsidian.js"
      ],
      "corpus": []
    },
    "test/deployment-config.test.js": {
      "what": "That every environment variable the application reads reaches a deployed instance through one of the two channels a deployment has - the image's own .env, written by the workflow and baked in by the Dockerfile, or the container's environment, written to deploy/.env and interpolated by docker-compose.yml - and that neither channel is wired up only halfway. Added after DIRECTORY_SERVICE_CLIENT_ID/_SECRET were read by the directory-search endpoint for two changes while nothing in the pipeline set them.",
      "guards": [
        ".github/workflows/docs-local.yml",
        "deploy/docker-compose.yml",
        "Dockerfile",
        "middlewares/directory-service.js",
        "app.js"
      ],
      "corpus": []
    }
  }
}
