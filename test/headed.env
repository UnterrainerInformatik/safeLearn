# Loaded by `npm run test:headed`. Runs the identical checks with a visible
# browser window, so a failure can be watched instead of read.
#
# This file exists instead of a shell prefix (`VAR=1 npm test`) so the switch
# works the same way on every platform. It holds no credentials.
SAFELEARN_TEST_HEADED=1
