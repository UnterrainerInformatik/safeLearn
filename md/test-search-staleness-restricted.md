@@@ teacher
# Search: a file that is relaxed after the last scan

A check removes the whole-file directive from this file while the application is
running and no scan follows, then searches for the word below. The index still
records this file as one only teachers may see; the answer must come from the
file as it now stands on disk, so the word must come back.

The word is STALEGESCHLOSSEN, and it appears in no other corpus file. The check
puts this file back the way it found it.
